[phases.setup]
nixPkgs = ['nodejs_22', 'python3', 'gcc', 'pkg-config']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm ci --prefix apps/web && cd apps/web && npm run build', 'npm ci --prefix apps/server && cd apps/server && npm run build']

[start]
cmd = 'cd apps/server && npm run start'
